<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vnstarzynski.movingtonewwest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.INTERNET" />

        <!-- The API key for Google Maps-based APIs. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAnXDV8sUfnh2mJFseE2lnm6UBiqFK72uE" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EducationActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".BusStopActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ShopActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SportsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EventActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EducationDetailActivity"
            android:parentActivityName=".EducationActivity" />

        <activity
            android:name=".FavouriteActivity"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EventDetailActivity"
            android:parentActivityName=".EventActivity" />

        <activity
            android:name=".SportsDetailActivity"
            android:parentActivityName=".SportsActivity" />

        <activity
            android:name=".ShopDetailActivity"
            android:parentActivityName=".ShopActivity" />

        <activity
            android:name=".BusStopDetailActivity"
            android:parentActivityName=".BusStopActivity" />
    </application>

</manifest>